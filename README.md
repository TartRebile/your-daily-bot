После клонирования репозитория используйте следующие команды для корректной работы бота:

python3 -m venv venv | source venv/bin/activate | pip install -r requirements.txt

Затем добавьте свой токен в файл "Main".
